# v0.2.0

- Sample the player path every 8 physics frames in `PlayLayer::postUpdate`
- Log jump/left/right presses and releases from `GJBaseGameLayer::handleButton` with position per attempt

# v0.1.0

- Foundation: Geode mod scaffold for `gdhelper.analyzer` (Windows, GD 2.2081)
- Record classic-level sessions to JSON (deaths, resets, completions)
- Tag the first normal-mode completion of a level as the reference run
